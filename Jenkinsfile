pipeline {
    agent any

    stages {
        stage("List files"){
            steps {
                // bishal
                sh 'pwd'
                sh 'ls -ltr'
            }
        }
    }
}
